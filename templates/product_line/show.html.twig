{% extends 'base.html.twig' %}

{% block title %}ProductLine{% endblock %}

{% block body %}
    <h1>ProductLine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_line.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product_line.description }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ product_line.quantity }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product_line.price }}</td>
            </tr>
            <tr>
                <th>Vat</th>
                <td>{{ product_line.vat }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_line.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_line_index') }}">back to list</a>

    <a href="{{ path('product_line_edit', {'id': product_line.id}) }}">edit</a>

    {{ include('product_line/_delete_form.html.twig') }}
{% endblock %}
