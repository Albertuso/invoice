{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}Listar Empresa{% endblock %}

{% block nav %}
{{ include('enterprise/shownavbar.html.twig') }}   
{% endblock %} 

{% block body %}
    <h1>Empresa</h1>

    <table class="table table-striped table-light">
  <thead>
    <tr>
      <th scope="col">Nombre</th>
      <th scope="col">Direccion</th>
      <th scope="col">Email</th>
      <th scope="col">Web</th>      
      <th scope="col">NIF</th>
      <th scope="col">Logo</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1</th>
      <td>{{ enterprise.name }}</td>
      <td>{{ enterprise.address }}</td>
      <td>{{ enterprise.email }}</td>  
      <td>{{ enterprise.web }}</td>
      <td>{{ enterprise.nif }}</td>
      <td>{{ enterprise.logo }}</td>
    </tr>    
  </tbody>
</table>

<a href="#">Supervisor</a><br/>
<a href="#">Pie de empresa</a></br>
<a href="#">RRSS</a>
</br></br></br>

    <a href="{{ path('enterprise_index') }}">Regresar al listado de empresas</a>

    <a href="{{ path('enterprise_edit', {'id': enterprise.id}) }}">Editar</a>

	<div class="container">
		<h1>Empresa</h1>
		<table class="table table-striped table-light">
			<thead>
				<tr>
					<th scope="col">Nombre</th>
					<th scope="col">Direccion</th>
					<th scope="col">Telefono</th>
					<th scope="col">Email</th>
					<th scope="col">Web</th>
					<th scope="col">NIF</th>
					<th scope="col">Logo</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<td>{{ enterprise.name }}</td>
					<td>{{ enterprise.address }}</td>
					<td>{{ enterprise.telephone }}</td>
					<td>{{ enterprise.email }}</td>
					<td>{{ enterprise.web }}</td>
					<td>{{ enterprise.nif }}</td>
					<td>{{ enterprise.logo }}</td>
				</tr>
			</tbody>
		</table>
	</div>
	<a href="#">Supervisor</a>
	<br>
		<a href="#">Pie de empresa</a>
	</br>
	<a href="#">RRSS</a>
	<br/><br/><br/><a href="{{ path('enterprise_index') }}">back to list</a>
	<a href="{{ path('enterprise_edit', {'id': enterprise.id}) }}"><br/>edit</a>
	{{ include('enterprise/_delete_form.html.twig') }}
{% endblock %}
