{% extends 'base.html.twig' %}

{% block title %}Editar red social{% endblock %}

{% block body %}
    <h1>Editar red social</h1>

    {{ include('network/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('network_index') }}">Regresar a redes sociales</a>

    {{ include('network/_delete_form.html.twig') }}
{% endblock %}
