{% extends 'base.html.twig' %}
{% block title %}Editar red
{% endblock %}

{% block nav %}
	{{ include('navbar/shownavbar.html.twig') }}
{% endblock %}

{% block body %}

	<div class="container">

		<h1>Editar red</h1>
		<div class="row">
			<div class="col-md-5">
				{{ include('network/_form.html.twig', {'button_label': 'Actualizar'}) }}
				<a href="{{ path('network_index', {'identerprise': enterprise.id}) }}">Regresar a redes sociales</a>
				{{ include('network/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
